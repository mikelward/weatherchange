# WeatherChange

Android-first personal weather briefing app.

This repository contains the Android MVP foundation for the app.

## Build

```sh
./gradlew assembleDebug
```

The build uses the Android SDK command-line tools directly, so set `ANDROID_HOME` or `ANDROID_SDK_ROOT` before running it.
